PyQt6>=6.6.0
httpx>=0.27.0
langdetect>=1.0.9
pyperclip>=1.8.2
pyinstaller>=6.0.0
Pillow>=10.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-qt>=4.4.0

# macOS post-event and listen-event permission APIs for native copy workflows.
pyobjc-framework-Quartz>=10.0.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=10.0.0; sys_platform == "darwin"

# Windows-only clipboard and native hook dependencies.
keyboard>=0.13.5; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
